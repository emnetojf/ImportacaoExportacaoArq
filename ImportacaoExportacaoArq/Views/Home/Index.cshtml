@{
    ViewData["Title"] = "Home Page";
}

@{
    ViewData["Title"] = "Lista de Clientes";
}

<h3>@ViewData["Title"]</h3>


<div class="custom-file">
    <input type="file" class="custom-file-input" id="customFile">
    <label class="custom-file-label" for="customFile">Choose file</label>
</div>

<table class="table table-striped table-dark">
    <thead>
        <tr>
            <th scope="col">ID</th>
            <th scope="col">Cliente</th>
            <th scope="col">E-mail</th>
            <th scope="col">Sexo</th>
            <th scope="col"></th>
        </tr>
    </thead>
    <tbody>

        @*@{

            foreach (var item in (List<Cliente>)ViewBag.ListaClientes)
            {

                <tr>

                    <td id="id">@Html.DisplayFor(modelItem => item.IdCliente) </td>
                    <td id="Nome">@Html.DisplayFor(modelItem => item.strNmCliente) </td>
                    <td>@Html.DisplayFor(modelItem => item.strEmail) </td>
                    <td>@Html.DisplayFor(modelItem => item.EnuSexo) </td>
                    <td>
                        <button class="btn btn-default btn-warning" type="button" onclick="Edit(@item.IdCliente)">Editar</button>

                        <button type="button" class="btn btn-danger" id="btn-delete"
                                data-toggle="modal" data-target="#modalDelete"
                                asp-action="Delete" asp-route-id="@item.IdCliente">
                            Excluir
                        </button>

                    </td>
                </tr>

            }
        }*@

    </tbody>
</table>


